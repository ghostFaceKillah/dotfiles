- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vimrc: vimrc
    ~/.bashrc: bash_profile
    ~/.tmux.conf: tmuxconf

- shell:
  - [git submodule update --init --recursive, Installing submodules]
